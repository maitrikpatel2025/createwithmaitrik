# Payload
PAYLOAD_SECRET=your-very-secret-key-change-in-production

# Database — SQLite is the default (no setup needed for dev)
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/maitrikpatel

# Email — ConvertKit or Beehiiv (add when ready to wire)
# EMAIL_PROVIDER=convertkit
# CONVERTKIT_API_KEY=
# CONVERTKIT_FORM_ID=

# Storage — local by default, S3/R2 for production
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_REGION=
# S3_ENDPOINT=
